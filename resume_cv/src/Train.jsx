function Train (props)
    { return(
        <div >
            <p className="heading">{props.heading}</p>
            <p>{props.title}</p>
            <p>{props.learn}</p>
        </div>
    );
    }
export default Train;